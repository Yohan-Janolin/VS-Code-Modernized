/* ============================================================
   Design tokens
   ============================================================ */
:root {
    --spacing: 0.35rem;
    --border-radius: 0.5rem;
    --tab-spacing: 0.2rem;
}

/* ============================================================
   Editor tabs
   ============================================================ */

/* Centrage vertical du texte de l'onglet */
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .tab-label {
    line-height: normal !important;
    display: flex !important;
    align-items: center !important;
    height: 100% !important;
}
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .tab-label .monaco-icon-label>.label-name {
    color: var(--vscode-tab-activeForeground, #ffffff) !important;
}

/* Forme : pilule arrondie avec bordure propre */
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab {
    border-radius: var(--border-radius);
    background-color: var(--vscode-tab-inactiveBackground, #171b26) !important;
    border: 2px solid var(--vscode-sideBar-border, rgb(0 0 0 / 21%)) !important;
    margin: 0 var(--tab-spacing);
}
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab.active {
    background-color: var(--vscode-list-hoverBackground, #343b4d) !important;
    border: 2px solid var(--vscode-tab-activeBorder, #ffffffc4) !important;
}

/* Suppression des indicateurs natifs haut/bas */
.tab-border-bottom-container,
.tab-border-top-container {
    background: none !important;
}

/* Espacement barre d'onglets */
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container {
    margin: 0 !important;
}
.monaco-workbench .part.editor>.content .editor-group-container>.title {
    margin-bottom: var(--spacing) !important;
}
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-and-actions-container {
    width: 100% !important;
}

/* Hover : neutralise le .tab-fill natif et reporte sur .tab entier */
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab>.tab-fill,
.modern-ui-tabs.monaco-workbench:not(:is(.hc-black, .hc-light)) .part.editor .tabs-container>.tab>.tab-fill {
    background: transparent !important;
    border: none !important;
}
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab:not(.active):hover {
    background-color: var(--modern-ui-editor-tab-hover-background, #2a3140) !important;
}

/* Zone d'actions (croix) : fond transparent au hover */
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .tab-actions-right,
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .tab-actions-right *,
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .monaco-action-bar,
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .close-action,
.monaco-workbench .part.editor>.content .editor-group-container>.title .tabs-container>.tab .codicon-close {
    background: transparent !important;
    background-color: transparent !important;
    border-radius: 0 !important;
}

/* NOTE : sélecteur natif malformé (trailing '>') — conservé tel quel, effet inconnu */
.monaco-workbench .part.editor>.content .editor-group-container> {
    border: 2px solid var(--vscode-editorGroup-border, #243443) !important;
}

/* ============================================================
   Editor group container
   ============================================================ */
.editor-group-container,
.monaco-workbench .part.editor>.content .editor-group-container {
    border-radius: var(--border-radius);
    overflow: hidden;
    height: calc(100% - var(--spacing)) !important;
    box-shadow: none;
}
.editor-container {
    border-radius: var(--border-radius) var(--border-radius) 0 0 !important;
    overflow: hidden !important;
}
.monaco-editor {
    border-radius: 0 0 var(--border-radius) var(--border-radius);
    overflow: hidden;
}
.monaco-editor-background {
    border-radius: var(--border-radius);
}
.monaco-editor .sticky-widget,
.monaco-editor .scroll-decoration {
    box-shadow: none;
}
.monaco-editor .find-widget {
    top: 12px !important;
    border-radius: var(--border-radius) !important;
}

/* ============================================================
   Editor column sizing
   ============================================================ */
.split-view-view>.monaco-grid-branch-node>.monaco-split-view2.vertical:has(#workbench\.parts\.editor) {
    width: calc(100% - var(--spacing)) !important;
}
.monaco-split-view2.horizontal>.monaco-scrollable-element>.split-view-container>.split-view-view:has(#workbench\.parts\.editor) {
    margin-left: var(--spacing);
}
#workbench\.parts\.editor>.content {
    background-color: unset !important;
    width: 100% !important;
}
.split-view-view.visible:has(>.part.editor) {
    background: #252a38 !important;
}
.split-view-view.visible:has(#workbench\.parts\.editor) {
    height: calc(100% - var(--spacing)) !important;
}
.split-view-view.visible,
.split-view-container {
    background: var(--vscode-activityBar-background, #171b26) !important;
}

/* ============================================================
   Floating panels — editor outer margins & border
   ============================================================ */
.monaco-workbench.floating-panels .monaco-grid-view .part.editor.floating-editor-outer-right,
.monaco-workbench.floating-panels .monaco-grid-view .part.editor.floating-editor-outer-top,
.monaco-workbench.floating-panels .monaco-grid-view .part.editor.floating-editor-outer-bottom,
.monaco-workbench.floating-panels .monaco-grid-view .part.editor.floating-editor-outer-left {
    margin: 0 !important;
}
.modern-ui .monaco-grid-view .part.editor:not(.modal-editor-part) {
    border: none !important;
    background-color: #171b26 !important;
}
.monaco-workbench .part.editor .shadow {
    display: none !important;
}

/* ============================================================
   Repl / debug console
   ============================================================ */
div.repl-tree.monaco-mouse-cursor-text.word-wrap {
    overflow: hidden !important;
}
.repl:has(div.repl-tree.monaco-mouse-cursor-text.word-wrap) {
    display: flex !important;
    flex-direction: column !important;
}

/* ============================================================
   Search results / generic lists
   ============================================================ */
.results>.monaco-list>.monaco-scrollable-element,
.results>.monaco-list>.monaco-scrollable-element>.monaco-list-rows {
    border-radius: 0 0 var(--border-radius) var(--border-radius) !important;
}
.monaco-list>.monaco-scrollable-element {
    border-radius: 0 0 var(--border-radius) var(--border-radius);
}
div.pane-body>div>div>div>div.monaco-list-rows {
    border-radius: 0 0 var(--border-radius) var(--border-radius);
    width: 97% !important;
    padding-bottom: 0.5rem;
}
div.monaco-scrollable-element>div.split-view-container>div>div>div.pane-body {
    background: var(--vscode-sideBar-background, #171b26) !important;
}
.monaco-pane-view .pane>.pane-header {
    padding: 0 6px;
}
.extensions-viewlet>.extensions .message-container {
    padding: unset !important;
}
.welcome-view-content {
    background: var(--vscode-editor-background, #171b26) !important;
}

/* ============================================================
   Sidebar / Explorer
   ============================================================ */
.monaco-workbench .part.sidebar {
    margin: 0 0 0 var(--spacing);
    border-radius: var(--border-radius);
    overflow: hidden !important;
    border-right: unset !important;
    outline-color: unset !important;
    height: calc(100% - var(--spacing)) !important;
    box-shadow: none;
}
.monaco-workbench .part.sidebar>.title {
    background: transparent;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
}
.monaco-workbench .part.sidebar>.title .title-label,
.monaco-workbench .part.sidebar>.title .title-label h2,
.monaco-workbench .part.sidebar>.title .title-label > span {
    line-height: 32px !important;
    margin: 0 !important;
    padding: 0 !important;
}
#workbench\.parts\.sidebar>.content {
    width: auto !important;
}
.monaco-workbench .part.sidebar>.content {
    background: var(--vscode-sideBar-background, #171b26) !important;
    border-radius: var(--border-radius) !important;
    overflow: hidden !important;
}
.monaco-workbench.floating-panels .part.sidebar {
    margin-right: var(--spacing) !important;
    margin-bottom: var(--spacing) !important;
}
.modern-ui-tabs.monaco-workbench.floating-panels .part.sidebar {
    background-color: var(--vscode-activityBar-background, #252a38) !important;
}

/* ============================================================
   Auxiliary bar (chat panel)
   ============================================================ */
.monaco-workbench .part.auxiliarybar>.title {
    background-color: unset !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
}
.monaco-workbench .part.auxiliarybar>.title .title-label,
.monaco-workbench .part.auxiliarybar>.title .title-label h2,
.monaco-workbench .part.auxiliarybar>.title .title-label > span {
    line-height: 32px !important;
    margin: 0 !important;
    padding: 0 !important;
}
.monaco-workbench .part.auxiliarybar {
    border: none !important;
    border-radius: var(--border-radius) !important;
    margin-left: calc(var(--spacing) * 2) !important;
    margin-right: var(--spacing) !important;
    height: calc(100% - calc(var(--spacing) * 2)) !important;
    box-shadow: none;
}
.split-view-view.visible:has(>.part.auxiliarybar) {
    margin-left: 0 !important;
}
.modern-ui.floating-panels :is(.part.sidebar,.part.auxiliarybar) {
    background-color: transparent !important;
}

/* ============================================================
   Panel (terminal / problems / output)
   ============================================================ */
.monaco-workbench .part.panel>.content {
    height: calc(100% - 2rem) !important;
}
.monaco-workbench .part.panel {
    margin: 0 var(--spacing) var(--spacing) 0;
    height: calc(100% - calc(var(--spacing) * 2 + 0.1rem)) !important;
    border-radius: var(--border-radius);
    display: flex;
    flex-direction: column;
    box-shadow: none;
    border-right: 2px solid var(--vscode-terminal-background, #171b2600) !important;
}
.monaco-workbench.floating-panels .part.panel {
    margin: 0 !important;
}
.part.panel.basepanel.bottom.pane-composite-part {
    margin-top: 0.1rem;
}
.monaco-workbench .part.panel.bottom .composite.title {
    border-top-style: none !important;
}
.terminal-group>.monaco-split-view2>.monaco-scrollable-element>.split-view-container>.split-view-view {
    background: var(--vscode-terminal-background, #171b26) !important;
}
.terminal.xterm,
.xterm-scrollable-element,
.xterm-scrollable-element>.xterm-screen,
.xterm-scrollable-element>.xterm-screen>.xterm-decoration-container {
    height: 100% !important;
}

/* ============================================================
   Panel separator lines
   ============================================================ */
.monaco-split-view2.separator-border.vertical>.monaco-scrollable-element>.split-view-container>.split-view-view:not(:first-child):before,
.monaco-split-view2.separator-border>.monaco-scrollable-element>.split-view-container>.split-view-view:not(:first-child):before {
    background: var(--vscode-panel-border, #252a38) !important;
}

/* ============================================================
   Title bar & status bar
   ============================================================ */
.monaco-workbench .part.titlebar>.titlebar-container>.titlebar-center>.window-title>.command-center .action-item.command-center-center {
    border-radius: 5rem;
    border: none;
}
.titlebar-center,
.titlebar-right {
    transition: all 0.15s;
}
.titlebar-center:hover,
.titlebar-right:hover {
    opacity: 1;
    pointer-events: all;
}
.part.statusbar {
    background-color: var(--vscode-statusBar-background, #252a38) !important;
}

/* ============================================================
   Legacy: activity bar in default left position
   ============================================================ */
li.action-item.icon.checked,
li.action-item.icon:hover {
    opacity: 1;
    border-radius: var(--border-radius);
    background-color: var(--vscode-actionBar-toggledBackground, #404245);
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .action-item {
    display: flex;
    justify-content: center;
    align-items: center;
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .badge .badge-content {
    z-index: 99 !important;
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .action-item.checked .active-item-indicator:before {
    border-left: none !important;
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .action-label.codicon {
    font-size: 20px !important;
    font-weight: bold;
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .action-label:not(.codicon) {
    padding: unset !important;
}
.monaco-workbench .activitybar>.content :not(.monaco-menu)>.monaco-action-bar .action-label {
    width: 44px !important;
    height: 44px !important;
}
li.action-item.icon {
    transition: all 0.15s;
    opacity: 0.2;
    margin-left: 0.3rem;
    padding-left: 0.05rem;
    margin-top: 0.2rem;
}
.monaco-workbench .pane-composite-part>.header-or-footer>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.checked .active-item-indicator:before,
.monaco-workbench .pane-composite-part>.header-or-footer>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item:focus .active-item-indicator:before,
.monaco-workbench .pane-composite-part>.title>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.checked .active-item-indicator:before,
.monaco-workbench .pane-composite-part>.title>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item:focus .active-item-indicator:before {
    border-top-width: 1px;
}

/* ============================================================
   Search view
   ============================================================ */
.search-view .search-widgets-container {
    margin: 0 calc(var(--border-radius) + 0.5rem) 0 var(--border-radius) !important;
}
div .search-view .search-widget .replace-input {
    width: 100% !important;
}
.search-view .search-widget .search-container {
    padding-bottom: 5px !important;
}
.search-view .monaco-findInput {
    width: 98% !important;
    border: 3px solid var(--vscode-checkbox-border, #6b6b6b) !important;
    border-radius: var(--border-radius) !important;
}
.replace-input>.monaco-findInput>.controls {
    margin: 1px 2px 0 0 !important;
}
.search-view .search-widget .replace-container {
    width: 93.3% !important;
}
.search-view .query-details.more h4 {
    padding: 4px 0px 4px 1px !important;
}
.monaco-findInput .monaco-inputbox {
    border-radius: var(--border-radius) !important;
}

/* ============================================================
   Command palette / quick input
   ============================================================ */
.quick-input-widget {
    transform: translateY(-50%) !important;
    top: 50% !important;
    z-index: 100;
    border: solid 2px var(--vscode-widget-border, #ffffff2d) !important;
    border-radius: 12px !important;
    font-size: 24px !important;
    box-shadow: 0px 8px 20px var(--vscode-widget-shadow, #0000005c) !important;
}
.quick-input-widget .monaco-list-rows {
    font-size: 13px !important;
    background: transparent !important;
}
.quick-input-widget .monaco-list-row:hover {
    background: var(--vscode-list-hoverBackground, #343b4d) !important;
}
.quick-input-filter .monaco-inputbox {
    border-radius: 12px !important;
    padding: 12px !important;
    border: none !important;
    background-color: var(--vscode-input-background, #171b26) !important;
    font-size: 20px !important;
    margin-bottom: 16px !important;
}
#quickInput_list>div>div.monaco-list-rows {
    width: calc(100% - 0.3rem) !important;
}
#command-blur {
    position: absolute !important;
    width: 100% !important;
    height: 100% !important;
    background: var(--vscode-editor-background, #171b26) !important;
    opacity: 80%;
    top: 0 !important;
    left: 0 !important;
    -webkit-backdrop-filter: blur(20px) !important;
    backdrop-filter: blur(20px) !important;
    z-index: 99 !important;
}

/* ============================================================
   Misc widgets
   ============================================================ */
.notification-toast,
.notification-toast>.notifications-list-container,
.notification-toast>.notifications-list-container>.monaco-list,
.monaco-scrollable-element,
.monaco-button,
.notifications-center.visible,
.chat-input-container,
.suggest-input-container {
    border-radius: var(--border-radius) !important;
}
.breadcrumbs-below-tabs {
    border-radius: var(--border-radius) var(--border-radius) 0 0;
    overflow: hidden;
    padding: 5px 0 0 0;
}
.monaco-breadcrumbs {
    font-size: 0.7rem;
}
div.monaco-workbench>div:has(iframe.webview.ready) {
    height: 100% !important;
}
.monaco-inputbox input::placeholder {
    color: var(--vscode-input-placeholderForeground, #cccccc80) !important;
}
.monaco-icon-label:after {
    font-size: 70% !important;
    margin: 1px 16px 0 5px !important;
}

/* ============================================================
   Custom activity bar (top position) — container
   ============================================================ */
.modern-ui-tabs .pane-composite-part> :is(.title, .header-or-footer)> .composite-bar-container> .composite-bar> .monaco-action-bar .action-item.icon {
    flex: 0 0 36px !important;
    width: 36px !important;
    min-width: 36px !important;
    max-width: 36px !important;
    padding: 0 6px !important;
}
.modern-ui .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon .action-label.codicon {
    font-size: 24px !important;
    line-height: 24px !important;
    color: #e08a3c !important;
}
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon .action-label:not(.codicon) {
    width: 24px !important;
    height: 24px !important;
    background-size: 24px !important;
    -webkit-mask-size: 24px !important;
    mask-size: 24px !important;
    background-position: center !important;
    -webkit-mask-position: center !important;
    mask-position: center !important;
    background-color: #e08a3c !important;
}

/* Conteneur global : fond, arrondi */
.monaco-workbench .pane-composite-part>.header-or-footer {
    background: var(--vscode-sideBar-background, #171b26) !important;
    border-radius: 0 0 var(--border-radius) var(--border-radius) !important;
    overflow: hidden !important;
}

/* ============================================================
   Custom activity bar (top position) — icon colors
   ============================================================ */
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon.checked .action-label.codicon,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon[aria-selected="true"] .action-label.codicon,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:hover .action-label.codicon {
    color: #ff6a00 !important;
}
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon.checked .action-label:not(.codicon),
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon[aria-selected="true"] .action-label:not(.codicon),
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:hover .action-label:not(.codicon) {
    background-color: #ff6a00 !important;
}
.modern-ui-tabs .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:not(.checked):not(:active):hover>.action-label.codicon {
    color: #ff6a00 !important;
}
.modern-ui-tabs .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:not(.checked):not(:active):hover>.action-label:not(.codicon) {
    background-color: #ff6a00 !important;
}

/* ============================================================
   Custom activity bar (top position) — remove native highlights
   ============================================================ */
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:hover,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon.checked,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon[aria-selected="true"] {
    background: transparent !important;
    background-color: transparent !important;
}
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon {
    --insert-border-color: transparent !important;
}
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon:hover,
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon.checked {
    box-shadow: none !important;
    outline: none !important;
}
.monaco-workbench .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container>.composite-bar>.monaco-action-bar .action-item.icon .active-item-indicator {
    background: transparent !important;
}
.modern-ui-tabs.monaco-workbench:not(.hc-black):not(.hc-light) .pane-composite-part>:is(.title,.header-or-footer)>.composite-bar-container:not(.dragged-over):not(.dragged-over-head):not(.dragged-over-tail)>.composite-bar>.monaco-action-bar .action-item.icon.checked:not(:active)>.active-item-indicator {
    background-color: transparent !important;
}

/* ============================================================
   Layout alignment — final overrides
   ============================================================ */

/* 1. Alignement bas uniforme des 3 colonnes */
.split-view-view.visible:has(#workbench\.parts\.sidebar),
.split-view-view.visible:has(#workbench\.parts\.auxiliarybar) {
    height: calc(100% - 2.35rem) !important;
    background: #252a38 !important;
}
#workbench\.parts\.sidebar,
#workbench\.parts\.auxiliarybar {
    height: 100% !important;
    margin-bottom: 0 !important;
}

/* 2. Marge droite du chat alignée sur celle de la sidebar */
.monaco-workbench .part.auxiliarybar {
    margin-right: var(--spacing) !important;
}

/* 3. Espace éditeur/terminal + compensation de hauteur */
.monaco-workbench.floating-panels .part.panel.bottom {
    margin-top: calc(var(--spacing) * 2) !important;
    height: calc(100% - (var(--spacing) * 4 + 0.1rem)) !important;
}

/* 4. Bordures du panneau neutralisées */
.monaco-workbench .part.panel,
.monaco-workbench.floating-panels .part.panel {
    border: none !important;
}

/* 5. Panneaux latéraux masqués : alignement symétrique */
.monaco-workbench.noauxiliarybar .monaco-grid-view .part.editor.floating-editor-outer-right {
    margin-right: var(--spacing) !important;
}
.monaco-workbench.noauxiliarybar .part.panel {
    margin-right: var(--spacing) !important;
}
.monaco-workbench.nosidebar .monaco-grid-view .part.editor.floating-editor-outer-left {
    margin-left: var(--spacing) !important;
}
.monaco-workbench.nosidebar .part.panel {
    margin-left: var(--spacing) !important;
}

/* 6. Ligne active en orange pâle */
.monaco-editor .view-overlays .current-line,
.monaco-editor .margin-view-overlays .current-line-margin {
    background-color: rgba(255, 160, 64, 0.12) !important;
    border: none !important;
}

/* 7. Quand sidebar ET chat masqués : éditeur centré */
.monaco-workbench.nosidebar.noauxiliarybar .monaco-split-view2.horizontal > .monaco-scrollable-element > .split-view-container > .split-view-view:has(#workbench\.parts\.editor) {
    margin-left: 0 !important;
}
.monaco-workbench.nosidebar.noauxiliarybar .split-view-view > .monaco-grid-branch-node > .monaco-split-view2.vertical:has(#workbench\.parts\.editor) {
    width: 100% !important;
}

/* 8. Chat : polish interne (barre "Sessions" + bas du chat) */
.monaco-workbench .part.auxiliarybar .agent-sessions-title-container {
    padding-right: 10px !important;
    box-sizing: border-box !important;
}
.monaco-workbench .part.auxiliarybar .chat-input-surface-stack {
    padding-bottom: 10px !important;
    box-sizing: border-box !important;
}